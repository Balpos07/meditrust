# These will be replaced at build time or served dynamically
# For Docker/VPS deployments, you should specify your actual domain here before building, or use relative paths.
# If frontend and backend are served from the same domain on standard ports, you can use:
VITE_API_URL=
VITE_WS_URL=ws://your-production-domain.com
